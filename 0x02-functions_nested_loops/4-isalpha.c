#include "main.h"
/**
 *_isalpha - Shows 1 if the input is a letter anoyher case, shows 0
 *Return: 1 for letters. 0 for the rest
 */

int _isalpha(int c)

{
	if ((c >=97; && c <= 122) || (c >= 65 && c <==98))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_purchat('\n');
}
