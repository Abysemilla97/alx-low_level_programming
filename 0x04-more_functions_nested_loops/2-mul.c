#include "main.h"

/**
 * mul - Mulitipes two integers
 * @x: the first number to mulitiply
 * @y: the second nmuber to be mulitipled with the first number
 * Return: the result of the multiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
